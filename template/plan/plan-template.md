# 实现计划: [功能名称]

**规范**: `../spec.md` | **分支**: `[xxx-功能名称]` | **日期**: [YYYY-MM-DD]

## 概要

[一句话：需求 + 技术方案]

---

## 技术上下文

> 默认继承当前项目技术栈，仅列出本功能特殊需求

| 项目 | 选择 | 备注 |
|------|------|------|
| 特殊依赖 | [如有] | [原因] |
| 新增工具 | [如有] | [原因] |

**性能目标**: [如: P95 < 200ms, 1000 QPS]

**约束**: [如: 必须兼容 XX]

---

## 项目结构

```
src/[功能目录]/
├── index.ts          # 入口
├── types.ts          # 类型
├── [主文件].ts       # 核心逻辑
└── [子模块]/
```

---

## 数据模型

```typescript
// 核心类型
interface [实体名] {
  id: string;
  [字段]: [类型];  // 说明
}
```

---

## API 契约

### [接口名]

| 属性 | 值 |
|------|-----|
| 方法 | POST |
| 路径 | `/api/xxx` |
| 请求 | `{ field: type }` |
| 响应 | `{ field: type }` |
| 错误 | 400: 参数错误, 404: 不存在 |

---

## 快速验证

```bash
# US1 验证
curl -X POST http://localhost:xxx/api/xxx -d '{}'
# 预期: 返回 200
```
